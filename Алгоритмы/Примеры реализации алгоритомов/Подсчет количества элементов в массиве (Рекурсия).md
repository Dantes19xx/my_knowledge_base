Пример реализации подсчета количества элементов в массиве [[Рекурсия|рекурсивным]] методом.

```python
def el_count(arr: list[int]) -> int :  
    if len(arr) == 1:  
        return 1  
  
    return 1 + el_count(arr[1:])
```


---
#алгоритмы #python 
