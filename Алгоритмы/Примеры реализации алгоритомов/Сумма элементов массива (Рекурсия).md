[[Рекурсия|Рекурсивная]] функция, которая реализует суммирование элементов массива.

```python
def sum_(arr: list[int]) -> int:  
    if not len(arr):  
        return 0  
  
    else:  
        return arr[0] + sum_(arr[1:])
```


---
#алгоритмы #python 