#flask #web #ахитектура_по 

## 1. Создание файла `config.py`

Вынеси все настройки в отдельный класс.  
Пример:

```python
import os
from dotenv import load_dotenv

# Загружаем .env файл (если есть)
load_dotenv()

class Config:
    # 🔑 Секретный ключ
    SECRET_KEY = os.getenv("SECRET_KEY", "dev-secret-key")

    # 🔗 Подключение к базе
    SQLALCHEMY_DATABASE_URI = os.getenv(
        "DATABASE_URL", "sqlite:///example.db"
    )

    # ⚙️ Настройки SQLAlchemy
    SQLALCHEMY_TRACK_MODIFICATIONS = False

    # 📩 Почтовый сервер (пример)
    MAIL_SERVER = os.getenv("MAIL_SERVER", "smtp.gmail.com")
    MAIL_PORT = int(os.getenv("MAIL_PORT", 587))
    MAIL_USE_TLS = os.getenv("MAIL_USE_TLS", "true").lower() in ["true", "1"]
    MAIL_USERNAME = os.getenv("MAIL_USERNAME")
    MAIL_PASSWORD = os.getenv("MAIL_PASSWORD")
```

👉 Здесь все значения берутся из **переменных окружения** (через `os.getenv`).  
👉 Если переменная не найдена, используется дефолтное значение (например, `sqlite:///example.db`).

## 2. Файл `.env` (для локальной разработки)

Создай `.env` в корне проекта:

```env
SECRET_KEY=my-very-secret-key
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=myemail@gmail.com
MAIL_PASSWORD=super-secret-password
```

## 3. Подключение конфигурации в приложении

В `app/__init__.py`:

```python
from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate
from .config import Config   # импортируем класс

db = SQLAlchemy()
migrate = Migrate()

def create_app():
    app = Flask(__name__)
    app.config.from_object(Config)   # загрузка конфигурации

    db.init_app(app)
    migrate.init_app(app, db)

    return app
```

## 4. Использование конфигурации внутри кода

В любом месте можно обратиться:

```python
from flask import current_app

print(current_app.config["SQLALCHEMY_DATABASE_URI"])
```

## 5. Хорошая практика

- Все конфиги — через `config.py`.
    
- Все чувствительные данные (пароли, ключи, URI) — через `.env` или переменные окружения.
    
- Для продакшена можно сделать разные классы:

```python
class DevelopmentConfig(Config):
    DEBUG = True

class ProductionConfig(Config):
    DEBUG = False
    SQLALCHEMY_DATABASE_URI = os.getenv("DATABASE_URL")
```

А потом при создании приложения подставлять:

```python
app.config.from_object("config.DevelopmentConfig")
```


---
## 📌 Итог

1. В `config.py` описываешь все переменные через `os.getenv`.
    
2. В `.env` задаёшь реальные значения.
    
3. В `app.config.from_object(Config)` подгружаешь настройки.
    
4. Flask-Migrate, SQLAlchemy и прочие расширения автоматически используют эти параметры.